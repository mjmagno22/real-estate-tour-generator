{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm install esbuild@0.28.1 --legacy-peer-deps && npm install --legacy-peer-deps",
  "env": {
    "NPM_CONFIG_ESBUILD": "false"
  }
}